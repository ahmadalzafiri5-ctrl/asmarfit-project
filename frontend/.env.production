# Used automatically by `npm run build` (Vite convention: .env.production
# overrides .env in production builds). This is what ends up baked into the
# Android app, since a phone can never reach http://localhost:3001.
VITE_API_BASE_URL=https://asmarfit-backend.onrender.com
